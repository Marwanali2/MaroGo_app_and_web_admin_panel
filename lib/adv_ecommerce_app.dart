import 'package:flutter/material.dart';

class AdvEcommerceApp extends StatelessWidget {
  const AdvEcommerceApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Container(),
    );
  }
}
